여기서 적절히 값을 구하는 건 어떻게 구현할 수 있을까?
직관적인 사고를 이용하면, 자손의 dp값을 내림차순으로 정렬한뒤에, 그 순서대로 인덱스를 부여하는 것(즉, 
i + dp[i]) 가 최댓값이 된다는 것을 추측할 수 있다. 그렇다면, 이게 왜 항상 참이 되는지 귀류법으로 알아보자.
결론을 부정해, Max+0이 최솟값이 아니라고 가정하자. 그렇게 된다면, dp[x]+0>Max+j가 된다, 하지만, dp[x]+0<Max+0<Max+j임이므로, dp[x]+0<Max+j가 성립하게 되고, 따라서 모순이 발생한다.
